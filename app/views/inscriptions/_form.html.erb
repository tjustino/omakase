<%= form_with(model: inscription, local: true) do |form| %>
  <% if inscription.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(inscription.errors.count, "error") %> prohibited this inscription from being saved:</h2>

      <ul>
        <% inscription.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :enfant_id %>
    <%= form.text_field :enfant_id %>
  </div>

  <div class="field">
    <%= form.label :encadrant_id %>
    <%= form.text_field :encadrant_id %>
  </div>

  <div class="field">
    <%= form.label :jour %>
    <%= form.number_field :jour %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
